org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
kotlin.incremental=false
# Kotlin incremental compilation disabled because Flutter pub cache (C:)
# and project source (D:) are on different drives — cross-drive path
# resolution fails in incremental caches.
